# Copy to .github/workflows/lint-pr.yml in the consuming repository.
name: Lint PR

on:
  pull_request_target:
    types: [opened, edited, synchronize, reopened]

permissions: {}

jobs:
  lint:
    permissions:
      pull-requests: read
    uses: PineappleEmperor/release-flow/.github/workflows/lint-pr.yml@709ad02eccec820cb882744c06a25ca3ea4a9169 # v1.0.0rc1

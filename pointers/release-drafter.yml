# Copy to .github/workflows/release-drafter.yml in the consuming repository.
# The all-zeros SHA is a placeholder: README.md, "The pointers".
name: Release Drafter

on:
  push:
    branches: [main]
  release:
    types: [published]

permissions:
  contents: write
  pull-requests: write

jobs:
  release:
    uses: PineappleEmperor/release-flow/.github/workflows/release-drafter.yml@0000000000000000000000000000000000000000 # v1.0.0
